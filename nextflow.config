// Path to your local savage conda environment
params.savage_env = "/opt/anaconda2/envs/savage"
// Length of the sequenced genome, used to calculate --split parameter for savage
params.ref_len = 2750

// Arguments for the savage assembler
params.mode = "denovo"
params.reads_dir = "/home/lejno/Desktop/WDV/reads_BBDukTrimmed/fastq"
params.out_dir = "." // Currently using directory other than "." results in savage not running error correction step. See https://github.com/HaploConduct/HaploConduct/issues/3
params.revcomp = "--revcomp" // Leave empty to not pass the --revcomp flag to savage
params.num_threads = 8
params.ref_file = "/home/lejno/Desktop/WDV/WDV_RefSeq.fasta"

// params.overlap_len_stage_c = 100
// params.merge_contigs = 0
// params.min_overlap_len =

